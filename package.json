{
  "name": "react-lzloader",
  "version": "0.0.1",
  "description": "Lazy-Load your components with Intersection Observer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["Lazy load", "Infinite scroll", "Intersection observer", "react"],
  "author": "kobayang",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "@types/react": "^16.0.38",
    "intersection-observer": "^0.5.0",
    "react": "^16.2.0"
  }
}
